# ============================================================
# FaceAuth — CORE Stable Dependencies
# ============================================================

# --- Web Framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Database ---
sqlalchemy==2.0.36
aiosqlite==0.20.0

# --- Authentication & Security ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
cryptography==44.0.0
pyotp==2.9.0
qrcode[pil]==8.0

# --- Environment & Utilities ---
python-dotenv==1.0.1
pydantic-settings==2.7.1
python-dateutil==2.9.0
apscheduler==3.10.4

# --- Face Recognition (Core) ---
# Loose versions allow pip to find the best match for your OS
Pillow>=10.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0

# --- AI & Biometrics (Note: These may require C++ compilers) ---
# If dlib fails, FaceAuth will automatically use OpenCV fallback
# dlib
# face-recognition

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
